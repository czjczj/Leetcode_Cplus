#include <iostream>
#include <string>
#include <stack>
#include <vector>
using namespace std;
int main(){
    vector<int> a;
    a.insert(a.begin(),1);
    a.insert(a.begin(),2);
    cout<<a[0];
    return 0;
}